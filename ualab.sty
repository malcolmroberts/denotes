%%
%% ualab.sty - definitions and defaults for lab manuals
%%
%%

\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{framed}
\usepackage[margin=1in,letterpaper,dvips]{geometry}
\usepackage{graphicx}
\usepackage{units}

%%
%% paragraph layout
%%

\setlength{\parskip}{3ex plus 1ex minus 1ex}
\setlength{\parindent}{0pt}

\numberwithin{equation}{section}

\newenvironment{enumerate*}{%
  \vspace{-\parskip}
  \begin{enumerate}
    \setlength{\itemsep}{2pt}
    \setlength{\parskip}{0pt}
    \setlength{\parsep}{0pt}}
  {\end{enumerate}}

\newcommand{\solstep}[1]{\hspace{-0.15in} \emph{\textbf{#1}}}

%%
%% need this stuff to compile for now...
%%

\newtheorem{theorem}{Theorem}
\newcommand{\oexample}[2]{\begin{example} #1 \end{example} \begin{solution} #2 \end{solution}}

%%
%% some math shortcuts
%%

\def\abs#1{\left|#1\right|}

\def\({\left(}\def\){\right)}
\def\[{\left[}\def\]{\right]}
\def\half{\frac{1}{2}}

\def\a{\alpha}
\def\b{\beta}
\def\c{\chi}
\def\d{\delta} \def\D{\Delta}
\def\e{\epsilon} \def\eps{\epsilon}
\def\g{\gamma} \def\G{\Gamma}
\def\h{\eta}
\def\i{\iota} \def\io{\iota}
\def\k{\kappa}
\def\l{\lambda} \def\L{\Lambda}
\def\n{\eta}
\def\Om{\Omega}
\def\p{\phi}\def\ph{\varphi} \def\P{\Phi}
\def\ps{\psi}\def\Ps{\Psi}
\def\r{\rho}
\def\s{\sigma} \def\S{\Sigma}
\def\t{\tau}
\def\th{\theta}\def\Th{\Theta}
\def\u{\upsilon} \def\U{\Upsilon}
\def\W{\Omega}
\def\w{\omega}
\def\x{\xi} \def\X{\Xi}
\def\z{\zeta}

\def\O{{\cal O}}

\def\Z{{\mathbb Z}}
\def\N{{\mathbb N}}
\def\R{\mathbb R}
\def\C{\mathbb C}
\def\Q{\mathbb Q}
\def\Re{\mathop{\rm Re}\nolimits}
\def\Im{\mathop{\rm Im}\nolimits}

%%
%% derivatives etc
%%

\newcommand{\pp}[2]{\frac{\partial #1}{\partial #2}}
\newcommand{\pptwo}[2]{\frac{\partial^2 #1}{\partial #2^2}}
\newcommand{\dd}[2]{\frac{d #1}{d #2}}
\newcommand\ddtwo[2]{\frac{d^2 #1}{d #2^2}}
\newcommand{\Laplace}[0]{\mathcal{L}}

\newcommand{\ddt}[1]{\dd{#1}{t}}
\newcommand{\ppt}[1]{\pp{#1}{t}}

%%
%% mktitle
%%

\newcommand{\mktitle}[3]{\begin{center}
    \textsf{\LARGE #1}\\
    \vspace{0.2in}
    \textsf{\LARGE #2}
    \vspace{2in}

    \textsf{\Huge #3}
    \vspace{2.5in}

    \textsf{\Large by GAME (Graduates at Alberta Mathematics Etc.)}
  \end{center}}

%%
%% example environment
%%

\newcounter{examplecounter}[section]

\newenvironment{example}%
{%
  \stepcounter{examplecounter}
  \bigskip
  \begin{framed}
    {\bf Example \arabic{chapter}.\arabic{section}.\arabic{examplecounter}: }
}{%
  \end{framed}
}

\newenvironment{easyexample}%
{%
  \stepcounter{examplecounter}
  \bigskip
  \begin{framed}
    {\bf Illustrative Example \arabic{chapter}.\arabic{section}.\arabic{examplecounter}: }
}{%
  \end{framed}
}

\newenvironment{example*}%
{%
  \bigskip
  \begin{framed}
    {\bf Example: }
}{%
  \end{framed}
}

\newenvironment{solution}%
{%
  {\bf Solution} \vspace{-\parskip}
  \begin{list}{}%
  {\setlength{\leftmargin}{0.25in}
  \setlength{\rightmargin}{0.25in}}
  \item[]%
}{%
  \qed
  \end{list}
}

%%
%% hide solutions environment
%%

\newif\ifshowsolutions

\newcommand{\hidesolution}[1]{%
  \ifshowsolutions%
    #1
  \else%
    {}%
  \fi%
}

%%
%% heads up! environment
%%

\newenvironment{heads}{%
  \textbf{Heads up! }}{}

%%
%% other
%%

% XXX: on memmett's machine, this has to appear here (ie, not at the
% top of this .sty) for some reason, otherwise 201.tex doesn't
% compile.  weird.
\usepackage{breqn}
